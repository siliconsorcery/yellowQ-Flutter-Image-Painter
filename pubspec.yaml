name: image_painter 
description: package - A flutter package for drawing over images. Supports color,
  strokeWidth, different paintModes and exporting image.
version: 0.5.0+1
homepage: https://github.com/yellowQ-software/yellowQ-Flutter-Image-Painter

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.1
